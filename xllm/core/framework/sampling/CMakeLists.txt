include(cc_library)

cc_library(
  NAME
    json_object_grammar
  HDRS
    json_object_grammar.h
  SRCS
    json_object_grammar.cpp
  DEPS
    :tokenizer
    :common
    glog::glog
    torch
    $<$<BOOL:${USE_NPU}>:tilelang_kernels>
    $<$<BOOL:${USE_NPU}>:unified_dlog>
)

cc_library(
  NAME 
    sampler
  HDRS
    sampling_params.h
    logits_utils.h
    rejection_sampler.h
    sampler.h
    rec_sampler.h
    beam_searcher.h
    rec_constrained_decoding.h
  SRCS
    sampling_params.cpp
    logits_utils.cpp
    rejection_sampler.cpp
    sampler.cpp
    rec_sampler.cpp
    beam_searcher.cpp
    rec_constrained_decoding.cpp
  DEPS
    :json_object_grammar
    :state_dict
    :common
    glog::glog
    torch
    :kernels
    $<$<BOOL:${USE_NPU}>:xllm_ops>
)
